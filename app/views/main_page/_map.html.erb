<%= turbo_frame_tag "main_view" do %>
    <%= javascript_include_tag "main_page" %>

    <div class="flex mb-6">
        <%= render "filters"%>
        <%= link_to 'Показать список', event_list_path, data: { turbo_frame: "main_view" }, class:"border-2 border-gray-500 px-4 py-2 font-semibold bg-transparent rounded-lg hover:text-gray-900 focus:text-gray-900 hover:bg-gray-200 focus:bg-gray-200 focus:outline-none focus:shadow-outline" %>
    </div>

    <div id="map" class="map w-full h-full"></div>
<% end %>
